{
  "mcpServers": {
    "deep-review-mcp": {
      "type": "stdio",
      "command": "uv",
      "args": [
        "run",
        "--no-sync",
        "--directory",
        "${CODEBUDDY_PLUGIN_ROOT}/deep-review-mcp",
        "deep-review-mcp"
      ],
      "cwd": "${CODEBUDDY_PLUGIN_ROOT}"
    }
  }
}
